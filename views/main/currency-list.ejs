<!-- 
	Note to self: Eventually I'll need to figure out a way to add an 
	arbitrary amount of containers to the view. This is probably easy to
	do; it just involves front-end stuff I don't understand.

	Theory: Load an empty view and populate it with dynamic divs elsewhere.
	This file will probably end up being pretty small (header and a container
	for the coin list)
-->

<div class="container">
	<h1>Currencies</h1>
	<a href="#" class="waves-effect waves-light btn" onclick="document.refreshPrices.submit();">
        <i class="material-icons left">trending_up</i>
        Refresh Prices
    </a>
    
    <% prices.forEach(function(price){ %>
    	<%= price.pair %>
        <%= price.exchange %>
        <%= price.last %>
        <%= price.highest %>
        <%= price.lowest %>
        <%= price.volume %>
    <% }); %>
</div>


	